---
phase: 05-vps-deployment
plan: 04
type: execute
wave: 4
depends_on:
  - 05-03
files_modified:
  - deploy.sh
  - docs/vps-setup.md
autonomous: true
gap_closure: true
requirements:
  - DEPLOY-02
  - DEPLOY-05
must_haves:
  truths:
    - "deploy.sh runs npm rebuild between npm ci and npm run build"
    - "native modules (node-pty, bcrypt) recompile for the VPS Node.js ABI on every deploy"
    - "docs/vps-setup.md Build the app section includes npm rebuild with an explanation"
  artifacts:
    - path: "deploy.sh"
      provides: "Zero-downtime update script with native module rebuild"
      contains: "npm rebuild"
    - path: "docs/vps-setup.md"
      provides: "VPS setup guide with native rebuild step documented"
      contains: "npm rebuild"
  key_links:
    - from: "deploy.sh"
      to: "node-pty / bcrypt native binaries"
      via: "npm rebuild after npm ci"
      pattern: "npm rebuild"
---

<objective>
Close the one verification gap found in phase 05: deploy.sh is missing `npm rebuild` after `npm ci`. This step recompiles native C++ modules (node-pty, bcrypt) against the Node.js ABI on the VPS. Without it, any Node.js version upgrade on the VPS will cause a MODULE_VERSION mismatch crash at startup.

Purpose: Make deploy.sh safe against Node.js version upgrades by ensuring native modules always match the running ABI.
Output: Updated deploy.sh (git pull → npm ci → npm rebuild → npm run build → pm2 reload) and updated docs/vps-setup.md with npm rebuild in the initial build section.
</objective>

<execution_context>
@C:/Users/mithu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mithu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-vps-deployment/05-VERIFICATION.md
@.planning/phases/05-vps-deployment/05-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add npm rebuild step to deploy.sh</name>
  <files>deploy.sh</files>
  <read_first>
    - deploy.sh (read the exact current contents before editing — the step labels, APP_NAME, and structure must be preserved)
  </read_first>
  <action>
The current deploy.sh has four numbered steps: [1/4] git pull, [2/4] npm ci, [3/4] npm run build, [4/4] pm2 reload.

Insert `npm rebuild` as a new step between npm ci and npm run build. The step count becomes 5 and all labels must update accordingly.

The final deploy.sh must be exactly:

```bash
#!/usr/bin/env bash
set -euo pipefail

# deploy.sh — Update the Claude VPS Interface to the latest git commit.
# Run this script on the VPS: SSH in, then run ./deploy.sh
#
# What this does:
#   1. Pull latest code from git
#   2. Install/update dependencies (npm ci — reproducible, uses package-lock.json)
#   3. Recompile native modules for current Node.js ABI (npm rebuild)
#   4. Compile TypeScript (npm run build → dist/)
#   5. Zero-downtime reload via pm2 (new process starts before old one dies)
#
# What this does NOT do:
#   - Touch environment secrets (set-and-forget after initial setup)
#   - Run database migrations (no database in this project)
#   - Restart Caddy (Caddy config is separate)

APP_NAME="claude-vps-interface"

echo "=== Deploy: $APP_NAME ==="
echo ""

echo "[1/5] Pulling latest code..."
git pull

echo "[2/5] Installing dependencies..."
npm ci

echo "[3/5] Rebuilding native modules..."
npm rebuild

echo "[4/5] Building..."
npm run build

echo "[5/5] Reloading pm2 process (zero-downtime)..."
pm2 reload "$APP_NAME"

echo ""
echo "Deploy complete. Check status with: pm2 status"
```

Use the Write tool to overwrite deploy.sh with this exact content. After writing, verify it is still executable:
  chmod +x deploy.sh

The `set -euo pipefail` at line 2 ensures that if npm rebuild fails (e.g., missing build-essential), the script exits before pm2 reload — the running process stays up.
  </action>
  <verify>
    <automated>bash -n deploy.sh && grep -q "npm rebuild" deploy.sh && grep -q "\[3/5\]" deploy.sh && grep -q "\[5/5\]" deploy.sh && grep -q "npm ci" deploy.sh && grep -q "npm run build" deploy.sh && grep -q "pm2 reload" deploy.sh && test -x deploy.sh && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - deploy.sh contains "npm rebuild" as a standalone command on its own line
    - deploy.sh contains "[3/5] Rebuilding native modules..." echo line
    - deploy.sh step labels read [1/5] through [5/5] (not [1/4] through [4/4])
    - deploy.sh step order is: git pull → npm ci → npm rebuild → npm run build → pm2 reload
    - npm rebuild appears AFTER npm ci and BEFORE npm run build (grep -n "npm" deploy.sh shows this order)
    - deploy.sh first line is "#!/usr/bin/env bash"
    - deploy.sh second line is "set -euo pipefail"
    - deploy.sh is executable (test -x deploy.sh exits 0)
    - bash -n deploy.sh exits 0 (valid bash syntax)
    - deploy.sh does NOT contain any reference to ".env"
    - deploy.sh contains "pm2 reload" (NOT "pm2 restart")
    - deploy.sh contains APP_NAME="claude-vps-interface"
  </acceptance_criteria>
  <done>deploy.sh implements the correct 5-step deploy sequence: git pull → npm ci → npm rebuild → npm run build → pm2 reload. Native modules are recompiled on every deploy, preventing ABI mismatch crashes after Node.js upgrades.</done>
</task>

<task type="auto">
  <name>Task 2: Document npm rebuild in docs/vps-setup.md</name>
  <files>docs/vps-setup.md</files>
  <read_first>
    - docs/vps-setup.md (read the exact current contents — section 4 "Build the app" needs npm rebuild added between npm ci and npm run build, with an explanation)
  </read_first>
  <action>
The current docs/vps-setup.md section 4 "Build the app" contains:

    npm ci
    npm run build

Update section 4 to include npm rebuild between those two commands and explain why it is required. Replace the entire section 4 block with:

```markdown
## 4. Build the app

    npm ci
    npm rebuild
    npm run build

`npm ci` installs dependencies from package-lock.json. `npm rebuild` recompiles native modules (node-pty, bcrypt) against the Node.js ABI on this machine — required so native binaries match the installed Node.js version. `npm run build` compiles TypeScript to `dist/`.

Run `npm rebuild` any time the Node.js version changes on the VPS (e.g., after `apt upgrade nodejs`). The deploy script (deploy.sh) runs it automatically on every deploy.
```

The rest of the file (sections 1-3, 5-6, Firewall Summary) must remain unchanged. Use the Write tool to write the complete updated file.

The complete updated file content:

```markdown
# VPS Setup Guide

One-time setup for a fresh Ubuntu 22.04 LTS VPS. Run once before deploying the app.

## Prerequisites

- A fresh Ubuntu 22.04 LTS VPS
- SSH access as a user with sudo privileges
- The repo cloned to the VPS (e.g. `git clone <repo-url> ~/claude-vps-interface`)

## 1. Run setup.sh

From the cloned repo directory:

    chmod +x setup.sh
    ./setup.sh

This installs: git, build-essential, python3, Node.js 20, npm, pm2, Caddy, and configures ufw firewall (ports 22/80/443 only).

## 2. Install Claude Code

Claude Code is installed separately per Anthropic's official instructions:
https://docs.anthropic.com/en/docs/claude-code

Verify installation: `claude --version`

## 3. Create .env

The `.env` file is NOT committed to git. Create it manually on the VPS:

    cp .env.example .env
    nano .env

Required values:

| Variable | Description | Example |
|----------|-------------|---------|
| PASSWORD | Login password for the web UI | a-strong-password |
| JWT_SECRET | 64-char random hex string | generate with: openssl rand -hex 32 |
| PORT | Port the Node.js app listens on | 3000 |
| CLAUDE_CMD | Claude Code binary name or path | claude |
| ALLOWED_COMMANDS | Comma-separated list of allowed bash commands | bash,ls,cat,git |

Generate a JWT_SECRET:

    openssl rand -hex 32

The `.env` file is set-and-forget. The deploy script does NOT modify it.

## 4. Build the app

    npm ci
    npm rebuild
    npm run build

`npm ci` installs dependencies from package-lock.json. `npm rebuild` recompiles native modules (node-pty, bcrypt) against the Node.js ABI on this machine — required so native binaries match the installed Node.js version. `npm run build` compiles TypeScript to `dist/`.

Run `npm rebuild` any time the Node.js version changes on the VPS (e.g., after `apt upgrade nodejs`). The deploy script (deploy.sh) runs it automatically on every deploy.

## 5. Configure pm2

See docs/pm2-setup.md (created in Plan 02).

## 6. Configure Caddy

See docs/caddy-setup.md (created in Plan 03).

## Firewall Summary

| Port | Protocol | Access | Purpose |
|------|----------|--------|---------|
| 22 | TCP | Public | SSH |
| 80 | TCP | Public | HTTP → Caddy redirects to 443 |
| 443 | TCP | Public | HTTPS (Caddy) |
| 3000 | TCP | Internal only | Node.js app (NOT exposed) |

Port 3000 is blocked by ufw. Caddy proxies from 443 → localhost:3000 internally.
```
  </action>
  <verify>
    <automated>grep -q "npm rebuild" docs/vps-setup.md && grep -q "node-pty" docs/vps-setup.md && grep -q "ABI" docs/vps-setup.md && grep -q "openssl rand" docs/vps-setup.md && grep -q "npm ci" docs/vps-setup.md && grep -q "npm run build" docs/vps-setup.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - docs/vps-setup.md contains "npm rebuild" as a command in section 4
    - docs/vps-setup.md contains "node-pty" in the explanation (naming the native module)
    - docs/vps-setup.md contains "ABI" in the explanation (naming the technical reason)
    - docs/vps-setup.md contains "bcrypt" in the explanation (naming the second native module)
    - docs/vps-setup.md still contains "openssl rand -hex 32" (not accidentally deleted)
    - docs/vps-setup.md still contains all five env var names: PASSWORD, JWT_SECRET, PORT, CLAUDE_CMD, ALLOWED_COMMANDS
    - docs/vps-setup.md still contains "https://docs.anthropic.com" (Claude Code URL)
    - docs/vps-setup.md still contains the Firewall Summary table with port 3000 as "Internal only"
    - In section 4, npm rebuild appears AFTER npm ci and BEFORE npm run build
    - docs/vps-setup.md mentions deploy.sh runs npm rebuild automatically
  </acceptance_criteria>
  <done>docs/vps-setup.md section 4 documents the npm ci → npm rebuild → npm run build sequence with an explanation of why npm rebuild is required (ABI matching for node-pty and bcrypt). Operators running a fresh install or upgrading Node.js know exactly what to do and why.</done>
</task>

</tasks>

<verification>
After both tasks complete:
- `bash -n deploy.sh` exits 0
- `test -x deploy.sh` exits 0
- `grep -n "npm" deploy.sh` output shows npm ci before npm rebuild before npm run build (line numbers in that order)
- `grep "\[3/5\]" deploy.sh` returns "Rebuilding native modules"
- `grep "npm rebuild" docs/vps-setup.md` returns a line in the build section
- `grep "node-pty\|bcrypt" docs/vps-setup.md` returns a line (native modules named in explanation)
- `grep "ABI" docs/vps-setup.md` returns a line (technical reason documented)
- All previously passing checks from 05-VERIFICATION.md remain green (deploy.sh still has git pull, npm ci, npm run build, pm2 reload, set -euo pipefail, no .env reference)
</verification>

<success_criteria>
- deploy.sh runs: git pull → npm ci → npm rebuild → npm run build → pm2 reload (5 steps)
- native modules recompile on every deploy — no ABI mismatch crashes after Node.js upgrades
- docs/vps-setup.md initial build section matches the deploy script sequence and explains why npm rebuild is needed
- All 11 previously passing verification truths remain satisfied
- Verification truth 12 ("deploy.sh handles native module rebuild after npm ci") now passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-vps-deployment/05-04-SUMMARY.md`
</output>
