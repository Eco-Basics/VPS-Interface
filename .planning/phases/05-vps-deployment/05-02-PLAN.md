---
phase: 05-vps-deployment
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - ecosystem.config.js
  - docs/pm2-setup.md
autonomous: true
requirements:
  - DEPLOY-02
  - DEPLOY-04
must_haves:
  truths:
    - "ecosystem.config.js exists in project root with correct pm2 app config"
    - "pm2 app name is exactly 'claude-vps-interface'"
    - "pm2 runs 'node dist/src/server.js' as the app entry point"
    - "pm2 startup + pm2 save are documented for reboot survival"
    - "docs/pm2-setup.md guides through first-time pm2 registration and ongoing management"
  artifacts:
    - path: "ecosystem.config.js"
      provides: "pm2 process config — version-controllable"
      contains: "claude-vps-interface"
    - path: "docs/pm2-setup.md"
      provides: "pm2 setup guide including startup and save commands"
      contains: "pm2 startup"
  key_links:
    - from: "ecosystem.config.js"
      to: "dist/src/server.js"
      via: "script field"
      pattern: "dist/src/server.js"
    - from: "docs/pm2-setup.md"
      to: "ecosystem.config.js"
      via: "pm2 start ecosystem.config.js"
      pattern: "pm2 start ecosystem.config.js"
---

<objective>
Create the pm2 ecosystem config (ecosystem.config.js) and companion pm2 setup guide (docs/pm2-setup.md) covering DEPLOY-02 (app running on VPS) and DEPLOY-04 (pm2 process management with reboot survival).

Purpose: pm2 manages the Node.js process lifetime — auto-restart on crash, zero-downtime reload, and reboot survival via pm2 startup + pm2 save.
Output: ecosystem.config.js (committed, used by deploy.sh), docs/pm2-setup.md (human guide for first-time registration and ongoing management).
</objective>

<execution_context>
@C:/Users/mithu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mithu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-vps-deployment/05-CONTEXT.md
@package.json
@.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ecosystem.config.js — pm2 process config</name>
  <files>ecosystem.config.js</files>
  <read_first>
    - package.json (confirms "start" script is "node dist/src/server.js" and app name is "claude-vps-interface")
    - .env.example (confirms PORT=3000 default; pm2 must inherit env vars from .env via dotenv in server.ts, not from pm2 env block)
    - .planning/phases/05-vps-deployment/05-CONTEXT.md (locked: pm2 app name = "claude-vps-interface", use ecosystem.config.js not bare CLI)
  </read_first>
  <action>
Create `ecosystem.config.js` in the project root.

The app reads its env vars via `import 'dotenv/config'` at the top of server.ts, so pm2 does NOT need to inject env vars — it just starts the process and the process loads .env itself. Do NOT put PASSWORD or JWT_SECRET in the pm2 env block.

Exact contents:

```javascript
module.exports = {
  apps: [
    {
      name: 'claude-vps-interface',
      script: 'node',
      args: 'dist/src/server.js',
      cwd: '/root/claude-vps-interface',
      instances: 1,
      autorestart: true,
      watch: false,
      max_memory_restart: '500M',
      error_file: '/root/.pm2/logs/claude-vps-interface-error.log',
      out_file: '/root/.pm2/logs/claude-vps-interface-out.log',
      log_date_format: 'YYYY-MM-DD HH:mm:ss',
      env: {
        NODE_ENV: 'production',
      },
    },
  ],
};
```

Notes on values:
- `name: 'claude-vps-interface'` — MUST match exactly; deploy.sh uses this name in `pm2 reload claude-vps-interface`
- `script: 'node'` + `args: 'dist/src/server.js'` — equivalent to `npm start` but more reliable under pm2 (avoids npm subprocess layer)
- `cwd: '/root/claude-vps-interface'` — assumes repo cloned to /root/claude-vps-interface; user adjusts if different path used
- `instances: 1` — single instance; no clustering (PTY sessions are stateful in-memory, not shareable across processes)
- `watch: false` — do NOT watch filesystem; deploy.sh triggers reload explicitly
- `max_memory_restart: '500M'` — safety valve for memory leaks in long-running sessions
- `NODE_ENV: production` — enables production behaviour in Express 5
- Secrets (PASSWORD, JWT_SECRET) are NOT in env block — loaded from .env by dotenv at startup
  </action>
  <verify>
    <automated>node -e "const c = require('./ecosystem.config.js'); const app = c.apps[0]; if (app.name !== 'claude-vps-interface') throw new Error('wrong name'); if (!app.args.includes('dist/src/server.js')) throw new Error('wrong script'); if (app.instances !== 1) throw new Error('wrong instances'); console.log('PASS');"</automated>
  </verify>
  <acceptance_criteria>
    - ecosystem.config.js exists at project root
    - ecosystem.config.js parses as valid CommonJS (node -e "require('./ecosystem.config.js')" exits 0)
    - ecosystem.config.js contains name: 'claude-vps-interface' (exact string)
    - ecosystem.config.js contains 'dist/src/server.js' in args field
    - ecosystem.config.js contains instances: 1
    - ecosystem.config.js contains autorestart: true
    - ecosystem.config.js contains watch: false
    - ecosystem.config.js does NOT contain "PASSWORD" or "JWT_SECRET" in env block
    - ecosystem.config.js contains NODE_ENV: 'production'
  </acceptance_criteria>
  <done>ecosystem.config.js exists with correct pm2 app configuration. pm2 app name matches what deploy.sh uses. Secrets are NOT embedded in the config.</done>
</task>

<task type="auto">
  <name>Task 2: Create docs/pm2-setup.md — pm2 first-time registration and management guide</name>
  <files>docs/pm2-setup.md</files>
  <read_first>
    - ecosystem.config.js (just created — guide commands must match its values)
    - .planning/phases/05-vps-deployment/05-CONTEXT.md (pm2 reload vs restart decision, startup+save requirement)
  </read_first>
  <action>
Create `docs/pm2-setup.md` covering first-time pm2 registration and common management tasks.

Exact structure:

```markdown
# pm2 Setup Guide

pm2 manages the Node.js process: auto-restart on crash, zero-downtime reload, and reboot survival.

## First-Time Registration

Run these commands once after the initial deploy (see docs/vps-setup.md for initial build steps):

    # Start the app for the first time
    pm2 start ecosystem.config.js

    # Verify it's running
    pm2 status

    # Configure pm2 to start on system reboot
    pm2 startup
    # ^ This prints a command like: sudo env PATH=... pm2 startup systemd -u root --hp /root
    # Copy and run that exact command

    # Save the current process list (so pm2 reloads it on reboot)
    pm2 save

After `pm2 save`, the app will automatically restart after any system reboot.

## Log Access

    # Live log tail
    pm2 logs claude-vps-interface

    # Last 100 lines
    pm2 logs claude-vps-interface --lines 100

    # Error log only
    tail -f ~/.pm2/logs/claude-vps-interface-error.log

## Restart vs Reload

| Command | Behaviour | When to use |
|---------|-----------|-------------|
| `pm2 reload claude-vps-interface` | Zero-downtime: new process starts before old one dies | Normal deploys (used by deploy.sh) |
| `pm2 restart claude-vps-interface` | Hard restart: brief downtime | After ecosystem.config.js changes |
| `pm2 stop claude-vps-interface` | Stop without removing | Temporary maintenance |
| `pm2 delete claude-vps-interface` | Remove from pm2 list | Deregistration only |

## Status Check

    pm2 status
    pm2 show claude-vps-interface

## After Editing ecosystem.config.js

If you modify `ecosystem.config.js` (e.g., change cwd or memory limit):

    pm2 restart ecosystem.config.js
    pm2 save

Use `pm2 restart` (not reload) when the config file itself changes.
```
  </action>
  <verify>
    <automated>grep -q "pm2 startup" docs/pm2-setup.md && grep -q "pm2 save" docs/pm2-setup.md && grep -q "pm2 start ecosystem.config.js" docs/pm2-setup.md && grep -q "claude-vps-interface" docs/pm2-setup.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - docs/pm2-setup.md exists
    - docs/pm2-setup.md contains "pm2 startup" (reboot survival step)
    - docs/pm2-setup.md contains "pm2 save" (persist process list step)
    - docs/pm2-setup.md contains "pm2 start ecosystem.config.js" (first-time start command)
    - docs/pm2-setup.md contains "pm2 reload claude-vps-interface" (zero-downtime reload)
    - docs/pm2-setup.md contains "pm2 logs claude-vps-interface" (log access)
    - docs/pm2-setup.md explains difference between reload and restart
  </acceptance_criteria>
  <done>docs/pm2-setup.md provides complete pm2 registration and management guidance. First-time setup (startup + save) is clearly documented. Log access and reload vs restart distinction are covered.</done>
</task>

</tasks>

<verification>
After both tasks:
- `node -e "require('./ecosystem.config.js')"` exits 0 (valid JS)
- `node -e "const c = require('./ecosystem.config.js'); console.log(c.apps[0].name)"` prints "claude-vps-interface"
- `grep "dist/src/server.js" ecosystem.config.js` returns a line
- `grep "PASSWORD\|JWT_SECRET" ecosystem.config.js` returns nothing
- `grep "pm2 startup" docs/pm2-setup.md` returns a line
- `grep "pm2 save" docs/pm2-setup.md` returns a line
</verification>

<success_criteria>
- ecosystem.config.js is a valid CommonJS module with correct app name, entry point, and production settings
- pm2 app name "claude-vps-interface" matches the name used in deploy.sh (Plan 03)
- No secrets in ecosystem.config.js — dotenv loads them at runtime
- docs/pm2-setup.md covers first-time registration (startup + save) and common management tasks
- After following the guide, the app restarts automatically on server reboot
</success_criteria>

<output>
After completion, create `.planning/phases/05-vps-deployment/05-02-SUMMARY.md`
</output>
