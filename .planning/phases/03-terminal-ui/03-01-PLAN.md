---
phase: 03-terminal-ui
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - public/index.html
  - public/app.js
  - public/style.css
  - src/app.ts
autonomous: true
requirements: [TERM-04, TERM-05, TERM-06]

must_haves:
  truths:
    - "A public/ scaffold exists with index.html, app.js, and style.css"
    - "index.html contains login-view, terminal-view, mobile-toolbar data-key buttons, new-session modal, and xterm CDN tags pinned to xterm@5.3.0 and xterm-addon-fit@0.8.0"
    - "style.css defines reset, login card, tab bar, status dots, terminal-wrapper active/inactive states, modal overlay, and mobile toolbar media query"
    - "app.js defines state, init, showLogin, showTerminal, clearAuthAndRedirect, apiFetch, createTab, switchTab, closeTab, and newSession scaffolds"
    - "src/app.ts mounts express.static before requireAuth"
  artifacts:
    - path: "public/index.html"
      provides: "Static HTML shell for login, tabs, modal, and mobile controls"
      exports: []
    - path: "public/style.css"
      provides: "Responsive Phase 3 styles"
      exports: []
    - path: "public/app.js"
      provides: "Vanilla JS frontend scaffold and auth/session stubs"
      exports: []
    - path: "src/app.ts"
      provides: "Static asset serving before auth middleware"
      exports: ["createApp"]
  key_links:
    - from: "public/index.html"
      to: "public/app.js"
      via: "script tag"
      pattern: "app\\.js"
    - from: "public/index.html"
      to: "xterm CDN"
      via: "stylesheet and script tags pinned to xterm@5.3.0 and xterm-addon-fit@0.8.0"
      pattern: "xterm@5\\.3\\.0"
    - from: "src/app.ts"
      to: "public/"
      via: "express.static mount"
      pattern: "express\\.static"
---

<objective>
Create the public frontend scaffold for Phase 3 and make it reachable from the backend. This wave does not finish terminal behavior; it establishes the HTML, CSS, JS structure and the static serving path required for later waves.

Output: `public/index.html`, `public/app.js`, `public/style.css`, and updated `src/app.ts`.
</objective>

<execution_context>
@C:/Users/mithu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mithu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/03-terminal-ui/03-CONTEXT.md
@.planning/phases/03-terminal-ui/03-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create the HTML shell and modal structure</name>
  <files>public/index.html</files>
  <action>
Create `public/index.html` with:

- a `login-view` section containing title, password input, submit button, and inline error area
- a hidden `terminal-view` section
- a top tab area with New Session button
- a `mobile-toolbar` with `data-key` buttons for `ctrl-c`, `esc`, `tab`, `up`, `down`, and `keyboard`
- a hidden `new-session` modal
- xterm.js and xterm-addon-fit CDN tags pinned to exact versions per locked decision in 03-CONTEXT.md

Required CDN tags (use these exact URLs — do not use floating versions or a different version number):

```html
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/xterm@5.3.0/css/xterm.css">
<script src="https://cdn.jsdelivr.net/npm/xterm@5.3.0/lib/xterm.js"></script>
<script src="https://cdn.jsdelivr.net/npm/xterm-addon-fit@0.8.0/lib/xterm-addon-fit.js"></script>
```

Required structural IDs/classes:

```html
<div id="login-view"></div>
<div id="terminal-view" hidden></div>
<div id="mobile-toolbar">
  <button data-key="ctrl-c">Ctrl+C</button>
  <button data-key="esc">Esc</button>
  <button data-key="tab">Tab</button>
  <button data-key="up">↑</button>
  <button data-key="down">↓</button>
  <button data-key="keyboard">Keyboard</button>
</div>
<div id="new-session-modal" hidden></div>
```
  </action>
  <verify>
    <automated>rg -n "login-view|terminal-view|mobile-toolbar|data-key|new-session-modal|xterm" public/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `public/index.html` exists
    - file contains `login-view`
    - file contains `terminal-view` with hidden initial state
    - file contains toolbar buttons with `data-key` attributes
    - file contains New Session modal markup
    - `rg "xterm@5\.3\.0" public/index.html` returns matches for both the CSS and JS CDN tags
    - `rg "xterm-addon-fit@0\.8\.0" public/index.html` returns a match for the fit addon CDN tag
    - no CDN tag references a floating version such as `xterm@5.x` or an unversioned URL
  </acceptance_criteria>
  <done>HTML scaffold exists and includes all required view roots, controls, and CDN dependencies pinned to xterm@5.3.0 and xterm-addon-fit@0.8.0</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create baseline responsive styles</name>
  <files>public/style.css</files>
  <action>
Create `public/style.css` with:

- reset and base typography rules
- login card styling
- compact tab bar styling with horizontal overflow
- `status-dot` variants
- `.terminal-wrapper.active` and `.terminal-wrapper.inactive`
- fixed bottom mobile toolbar hidden on desktop via media query
- modal overlay styles

Required selectors:

```css
* { box-sizing: border-box; }
.login-card { }
.tab-bar { overflow-x: auto; }
.status-dot.running { }
.status-dot.idle { }
.status-dot.exited { }
.terminal-wrapper.active { display: block; }
.terminal-wrapper.inactive { display: none; }
@media (max-width: 767px) { .mobile-toolbar { position: fixed; bottom: 0; } }
.modal-overlay { }
```
  </action>
  <verify>
    <automated>rg -n "login-card|tab-bar|status-dot|terminal-wrapper|mobile-toolbar|modal-overlay" public/style.css</automated>
  </verify>
  <acceptance_criteria>
    - reset styles exist
    - login card styles exist
    - tab bar uses horizontal overflow
    - status dot classes exist for running, idle, and exited
    - active and inactive terminal wrapper classes exist
    - mobile toolbar media query exists
    - modal overlay styles exist
  </acceptance_criteria>
  <done>Responsive Phase 3 style scaffold exists</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create frontend state and auth/session stubs</name>
  <files>public/app.js</files>
  <action>
Create `public/app.js` with:

- a `state` object containing `token`, `sessions`, and `activeSessionId`
- `init()` that checks `localStorage.getItem('vps_jwt')`
- `showLogin()` and `showTerminal()`
- `clearAuthAndRedirect()`
- `apiFetch()` with 401 handling
- stub functions `createTab`, `switchTab`, `closeTab`, and `newSession`
- login form submit handler posting to `/auth/login`
- final `init()` call

Each session record in `state.sessions` must include `idleTimer` in its shape so that plan 03-04 can attach the idle timer without modifying the state structure:

```javascript
// Session record shape (create with all fields present):
// { id, label, status, ws, terminal, fitAddon, tabEl, wrapperEl, idleTimer }
```

Required scaffold:

```javascript
const state = {
  token: localStorage.getItem('vps_jwt'),
  sessions: [],
  activeSessionId: null,
};

function showLogin() {}
function showTerminal() {}
function clearAuthAndRedirect() {}
async function apiFetch(path, options = {}) {}
function createTab(session) {}
function switchTab(sessionId) {}
function closeTab(sessionId) {}
function newSession() {}
async function init() {}

init();
```
  </action>
  <verify>
    <automated>rg -n "const state|function init|showLogin|showTerminal|clearAuthAndRedirect|apiFetch|createTab|switchTab|closeTab|newSession|/auth/login|init\(\)" public/app.js</automated>
  </verify>
  <acceptance_criteria>
    - `state` includes token, sessions, and activeSessionId
    - `init()` reads `vps_jwt`
    - login submit handler POSTs to `/auth/login`
    - `clearAuthAndRedirect` removes auth state
    - `apiFetch` handles 401
    - tab management functions exist as stubs
    - session record shape includes `idleTimer` field
    - file ends by calling `init()`
  </acceptance_criteria>
  <done>Frontend JS scaffold exists and login bootstrap is wired</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Mount static assets before auth middleware</name>
  <files>src/app.ts</files>
  <action>
Update `src/app.ts` to mount `express.static` for `public/` before `requireAuth`.

Required pattern:

```typescript
app.use(express.static(path.join(process.cwd(), 'public')));
app.use('/auth', createAuthRouter(passwordHash));
app.use(requireAuth);
```
  </action>
  <verify>
    <automated>rg -n "express\.static|requireAuth" src/app.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/app.ts` imports `path`
    - `express.static(...)` is mounted
    - static middleware appears before `requireAuth`
    - `/auth` remains reachable before auth middleware
  </acceptance_criteria>
  <done>Static frontend is served publicly without breaking existing auth routes</done>
</task>

</tasks>

<verification>
Run:

```bash
curl -i http://localhost:3000/
curl -i http://localhost:3000/app.js
curl -i http://localhost:3000/style.css
rg -n "xterm@5\.3\.0" public/index.html
rg -n "xterm-addon-fit@0\.8\.0" public/index.html
rg -n "express\.static|requireAuth" src/app.ts
```
</verification>

<success_criteria>
- `public/` scaffold exists
- root HTML and static assets are publicly served
- xterm CDN tags are pinned to `xterm@5.3.0` and `xterm-addon-fit@0.8.0`
- login bootstrap code exists
- backend serves frontend before JWT enforcement
</success_criteria>

<output>
After completion, create `.planning/phases/03-terminal-ui/03-01-SUMMARY.md`
</output>
